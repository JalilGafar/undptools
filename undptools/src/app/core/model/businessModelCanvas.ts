export class BusinessModelCanvas {
  id!: number;
  cons_id!: number;
  comp_id!: number;

  // Customer Segments (3 inputs)
  customerSegments1!: string;
  customerSegments2!: string;
  customerSegments3!: string;

  // Value Propositions (3 inputs)
  valuePropositions1!: string;
  valuePropositions2!: string;
  valuePropositions3!: string;

  // Channels (3 inputs)
  channels1!: string;
  channels2!: string;
  channels3!: string;
  
  // Probleme Identifie (3 inputs)
  problemeIdentifie1!: string;
  problemeIdentifie2!: string;
  problemeIdentifie3!: string;

  // But (3 inputs)
  but1!: string;
  but2!: string;
  but3!: string;

  // Mesure d'Impact (3 inputs)
  mesureImpact1!: string;
  mesureImpact2!: string;
  mesureImpact3!: string;

  // Customer Relationships (3 inputs)
  customerRelationships1!: string;
  customerRelationships2!: string;
  customerRelationships3!: string;

  // Revenue Streams (3 inputs)
  revenueStreams1!: string;
  revenueStreams2!: string;
  revenueStreams3!: string;

  // Key Resources (3 inputs)
  keyResources1!: string;
  keyResources2!: string;
  keyResources3!: string;

  // Key Activities (3 inputs)
  keyActivities1!: string;
  keyActivities2!: string;
  keyActivities3!: string;

  // Key Partnerships (3 inputs)
  keyPartnerships1!: string;
  keyPartnerships2!: string;
  keyPartnerships3!: string;

  // Cost Structure (3 inputs)
  costStructure1!: string;
  costStructure2!: string;
  costStructure3!: string;

  // Metadata
  date!: Date;
  createdAt!: Date;
  updatedAt!: Date;
}
